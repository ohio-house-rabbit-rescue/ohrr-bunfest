# Public, browser-safe Supabase config (the same project the OHRR app and the
# OHRR website use), so both the local preview and the Cloudflare Pages build
# connect with no dashboard setup. The publishable key is safe to commit;
# security is enforced by Row-Level Security. Never add the secret key.
VITE_SUPABASE_URL=https://ixxzebtzjgeimoijexwn.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable__Yc7IquvYLVFdgqfcXfvmw_vN_bEIyO
